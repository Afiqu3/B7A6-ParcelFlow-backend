enum Role {
    SUPER_ADMIN
    ADMIN
    MERCHANT
    RIDER
}

enum AccountStatus {
    ACTIVE
    BLOCKED
}

enum AuthProvider {
    GOOGLE
    CREDENTIAL
}

enum VehicleType {
    BIKE
    BICYCLE
    VAN
}

enum RiderApplicationStatus {
    PENDING
    APPROVED
    REJECTED
}
